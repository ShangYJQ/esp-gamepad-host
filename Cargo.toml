[package]
name = "esps3xboxctl"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "esps3xboxctl"
test = false
bench = false


[dependencies]
embassy-executor = "0.10.0"
embassy-time = "0.5.1"
embassy-usb-host = { version = "0.1.0", default-features = false, features = ["log"] }
esp-backtrace = { version = "0.20.0", features = ["esp32s3", "panic-handler", "println"] }
esp-bootloader-esp-idf = { version = "0.6.0", features = ["esp32s3"] }
esp-hal = { version = "1.2.2", features = ["esp32s3", "log-04", "unstable"] }
esp-println = { version = "0.18.0", features = ["esp32s3", "log-04"] }
esp-rtos = { version = "0.4.0", features = ["esp32s3", "embassy", "log-04"] }
log = "0.4.34"

[profile.release]
debug = true
debug-assertions = true
lto = "fat"
codegen-units = 1
